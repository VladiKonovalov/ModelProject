# ModelProject

In this code,
the objective is to build a machine learning model using TensorFlow to predict the "Year"
based on the "Population Growth Rate" and "Growth Rate" data from a given dataset. 
The dataset is stored in a CSV file named "Population_Growth.csv".
i got the csv file from the "Kaggler" - platform that share with you already ready datafile

